import { RechClientPipe } from './rech-client.pipe';

describe('RechClientPipe', () => {
  it('create an instance', () => {
    const pipe = new RechClientPipe();
    expect(pipe).toBeTruthy();
  });
});
